import ToolbarComponent from "../../common/components/layouts/WebpageWrapper/toolbar.component.jsx"

export default {
  title: "Toolbar",
  component: ToolbarComponent
}

export function Default(args) {
  return <ToolbarComponent {...args} />;
}