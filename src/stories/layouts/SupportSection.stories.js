import SupportSection from "../../common/components/layouts/HomepageContent/SupportSection/SupportSection.component.jsx"

export default {
  title: "SupportSection",
  component: SupportSection
}

export function Default(args) {
  return <SupportSection {...args} />;
}